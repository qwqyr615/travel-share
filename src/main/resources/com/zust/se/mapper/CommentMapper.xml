<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zust.se.mapper.CommentMapper">

    <resultMap id="commentWithUserMap" type="com.zust.se.model.Comment">
        <id property="id" column="id"/>
        <result property="postId" column="post_id"/>
        <result property="userId" column="user_id"/>
        <result property="createTime" column="create_time"/>
        <association property="user" javaType="com.zust.se.model.User">
            <result property="nickname" column="nickname"/>
            <result property="avatar" column="avatar"/>
        </association>
    </resultMap>

    <!-- 查询某游记的评论（带用户昵称和头像） -->
    <select id="findByPostIdWithUser" resultMap="commentWithUserMap">
        SELECT c.id, c.post_id, c.user_id, c.create_time,
               u.nickname, u.avatar
        FROM t_comment c
        LEFT JOIN t_user u ON c.user_id = u.id
        WHERE c.post_id = #{postId}
        ORDER BY c.create_time DESC
    </select>

    <!-- 查询某用户的所有评论（带游记标题，用于个人中心） -->
    <select id="findByUserIdWithPost" resultType="java.util.Map">
        SELECT c.*, p.title AS post_title
        FROM t_comment c
        LEFT JOIN t_post p ON c.post_id = p.id
        WHERE c.user_id = #{userId}
        ORDER BY c.create_time DESC
    </select>

</mapper>
